import React from "react";
import { View, Text, TextInput, StyleSheet } from "react-native";
import SearchBar from "../components/SearchBar";

const SearchScreen = () => {
  return (
    <View>
      <SearchBar/>
      <Text>Search Scre</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  
});

export default SearchScreen;
